# Copy this file to .env.local and adjust if your backend runs elsewhere.
# Not needed for local dev if you use `npm run dev`, which proxies /api
# to http://localhost:8000 automatically (see vite.config.js).
VITE_API_BASE=/api
